name: memohari
description: "Aplikasi catatan harian dengan pengingat"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter

  sqflite: ^2.3.0
  sqflite_common_ffi: ^2.3.0
  path_provider: ^2.1.0
  path: ^1.9.0
  intl: ^0.19.0
  flutter_local_notifications: ^17.0.0
  timezone: ^0.9.3
  android_alarm_manager_plus: ^4.0.0
  shared_preferences: ^2.2.3

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true